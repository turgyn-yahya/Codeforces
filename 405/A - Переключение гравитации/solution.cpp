#include <bits/stdc++.h>        #define pb push_back#define doub double#define F first#define S second#define clr clear()#define sz size()#define int long long#define str string#define ld long double#define pf push_front#define endl "\n" using namespace std;const int N = 1e5 + 13;int T = 1;bool issmall(char a){    if(a>=97 && a<=122){        return true;    }    else return false;}bool isbig(char a){if(a>=65 && a<=90){        return true;    }    else return false;}int b[101];int n,m,k,mx = -9999,mn = 9999,sum = 0,ans = 0,cnt = 0;void sorti(int a,int c){	if(b[a] > b[c]){		swap(b[a],b[c]);	}}signed main(){int T = 1;//    cin >> T;    stringstream s1;    s1.clr;// ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);    while(T--){	    cin >> n;	    for(int i=1;i <= n;i++){	    	cin >> b[i];		}		for(int i=1;i < n;i++){			for(int j=i+1;j <= n;j++){				sorti(i,j);			}		}	    for(int i=1;i <= n;i++){	    	cout << b[i] << " ";		}}}// 1 0 m 0       /*  i = высота    j = ширина*/ /*d[i] = min(d[i-1]+abs(h[i]-h[i-1])(Дп штрафы)*/     /*for (auto& str : a) {        std::cout << str << ' ';    }*/    /*cin >> n >> m;    for (int i = 1 i <= m; ++ i) {        int v, u; cin >> v >> u;        g[v].push_back (u), g[u].push_back (v);    }    for (int i = 2; i <= n; ++ i) dst[i] = INF;    dst[1] = 0;    queue <int> q;    q.push (1);    while (q.size()) {        int v = q.front();        q.pop();        for (int i = 0; i < g[v].size(); ++ i) {            int to = g[i][v];            if (dst[to] > dst[v] + 1) {                dst[to] = dst[v] + 1;                q.push (to);            }        }    }    for (int i = 1; i <= n; ++ i) cout << dst[i] << ' ';}*/   /*vector <int> g[N];int n, m, used[N], cnt; void dfs (int v) {    used[v] = 1;    for (int i = 0; i < g[v].size(); ++ i) {        int to = g[v][i];        if (!used[to]) {            dfs (to);        }    }    return;}*/    /*int n;vector<char> prime (n+1, true);prime[0] = prime[1] = false;for (int i=2; i<=n; ++i){    if (prime[i]){        if (i * 1ll * i <= n){            for (int j=i*i; j<=n; j+=i){                prime[j] = false;                }            }    }}            */                   /*string s;  cin >> s;  sort(s.begin(), s.end());  do    cout << s << endl;  while(next_permutation(s.begin(), s.end()));  return 0;*/